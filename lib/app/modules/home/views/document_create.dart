import 'package:flutter/material.dart';

class DocumentCreate extends StatelessWidget {
  const DocumentCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blueAccent,
      ),
    );
  }
}